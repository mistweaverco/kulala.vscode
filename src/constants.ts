export { DEFAULT_CORE_VERSION } from "./versions";

export const HTTP_FILETYPES = ["http", "rest"] as const;

export const TRIGGER_CHARS = ["@", "#", "-", ":", "{", "$", ">", "<", ".", "(", '"', "'"];
